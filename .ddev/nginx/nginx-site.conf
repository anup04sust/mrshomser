# Increase timeouts for Next.js API routes (especially for LLM streaming)
proxy_connect_timeout 300s;
proxy_send_timeout 300s;
proxy_read_timeout 300s;
send_timeout 300s;

# Enable buffering for streaming responses
proxy_buffering off;
proxy_cache off;
